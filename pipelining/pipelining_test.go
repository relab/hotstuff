package pipelining_test

import (
	"testing"
)

// Testing pipelining with all necessary modules in a simple scenario
func TestPipelining(t *testing.T) {
	// pipes := []pipelining.PipeId{1, 2, 3, 4, 5}
	// buidler := modules.NewBuilder(1, testutil.GenerateECDSAKey(t))
	// buidler.Add(
	// 	blockchain.NewPiped(pipes),
	// 	eventloop.New(100),
	// )
	//
	// buidler.Build()
}
